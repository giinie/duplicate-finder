# This is a comment
! This is also a comment

# Empty lines should be ignored

# Short value (should be ignored if shorter than minLength)
short.key=abc

# Normal values
test.key1=This is a test value
test.key2=Another test value with more content

# Value with special characters
special.key=Value with = equals sign and # hash symbol

# Value with leading/trailing spaces
spaces.key=   Value with spaces   

# Empty value (should be ignored)
empty.key=

# Just a key without value (should be ignored)
invalid.key

# Multiple equal signs
multiple.equals=first=second=third